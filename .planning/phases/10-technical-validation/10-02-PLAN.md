---
phase: 10-technical-validation
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - generate-sitemap.js
  - public/sitemap.xml
autonomous: true

must_haves:
  truths:
    - "Sitemap contains all 216+ page URLs with no missing or extra pages"
    - "Hub pages have higher priority (0.8-0.9) than spoke pages (0.7)"
    - "Homepage has priority 1.0 and weekly changefreq"
    - "Sitemap URL count matches page.tsx count exactly"
    - "No redirected or deleted page URLs appear in sitemap"
  artifacts:
    - path: "generate-sitemap.js"
      provides: "Enhanced sitemap generator with differentiated priorities"
      contains: "getPriority"
    - path: "public/sitemap.xml"
      provides: "Complete sitemap with all pages and correct priorities"
      contains: "urlset"
  key_links:
    - from: "generate-sitemap.js"
      to: "public/sitemap.xml"
      via: "scans app/ and writes XML"
      pattern: "writeFileSync.*sitemap"
---

<objective>
Enhance the sitemap generator with differentiated priorities for hub vs spoke pages, regenerate the sitemap to include all pages added/removed across Phases 1-9, and rebuild the site to include the updated sitemap.

Purpose: The current sitemap is 15 pages behind and uses flat 0.6 priority for everything. Google needs accurate signals about page importance for efficient crawling.
Output: Updated generate-sitemap.js with smart priorities, fresh public/sitemap.xml with all 216+ URLs, clean rebuild including the new sitemap.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-technical-validation/10-RESEARCH.md
@.planning/phases/10-technical-validation/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance sitemap generator with differentiated priorities</name>
  <files>generate-sitemap.js</files>
  <action>
    Update `generate-sitemap.js` to use smart priority and changefreq values:

    1. Add a `getPriority(route)` function with this hierarchy:
       - `/` (homepage): `1.0`
       - `/services`, `/commercial-roofing`, `/residential-roofing`, `/service-areas`: `0.9` (main navigation hubs)
       - Service hub pages (`/roof-repair/`, `/hail-damage-repair/`, `/roof-replacement/`, `/storm-damage-repair/`, `/metal-roofing/`, `/wind-damage-repair/`, `/tpo-roofing/`, `/roof-inspections/`, `/asphalt-shingle-roofing/`): `0.8`
       - City hub pages (match pattern `/{city}-texas-roofing` or `/{city}-tx-roofing`): `0.8`
       - Service-city spoke pages (the bulk — `/roof-repair-amarillo/`, etc.): `0.7`
       - Everything else (`/about`, `/contact`, `/reviews`, `/gallery`, `/blog/*`): `0.5`

    2. Add a `getChangefreq(route)` function:
       - Homepage: `weekly`
       - Hub pages (priority 0.8-0.9): `monthly`
       - Everything else: `monthly`

    3. Keep existing behavior:
       - Still scans `app/` for `page.tsx` files
       - Still filters out `_not-found` and `api/` routes
       - Still sorts routes alphabetically
       - Still uses `https://5starroofingpros.com` as base URL
       - Still adds trailing slash to non-root routes

    4. Add a summary log at the end showing count by priority tier:
       ```
       Sitemap generated with N routes
         Priority 1.0: 1 (homepage)
         Priority 0.9: 4 (main hubs)
         Priority 0.8: N (service + city hubs)
         Priority 0.7: N (spoke pages)
         Priority 0.5: N (other)
       ```

    5. Replace the hardcoded `'0.6'` and `'monthly'` with calls to the new functions.
  </action>
  <verify>
    ```
    node generate-sitemap.js
    ```
    Output shows route count matching expected ~216 and a breakdown by priority tier. Inspect public/sitemap.xml to confirm:
    - Homepage has priority 1.0 and changefreq weekly
    - /services/ has priority 0.9
    - /roof-repair/ has priority 0.8
    - /roof-repair-amarillo/ has priority 0.7
    - /about/ has priority 0.5
  </verify>
  <done>generate-sitemap.js uses differentiated priorities, sitemap URL count matches page.tsx count, priority tiers are correctly assigned.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate sitemap and rebuild site</name>
  <files>public/sitemap.xml</files>
  <action>
    1. Run the enhanced sitemap generator:
       ```
       node generate-sitemap.js
       ```

    2. Verify the URL count matches page.tsx count. Count page.tsx files:
       ```
       find app -name "page.tsx" | grep -v _not-found | grep -v api | wc -l
       ```
       The sitemap URL count must match this number exactly.

    3. Spot-check the sitemap XML for correctness:
       - No duplicate URLs
       - No URLs for deleted/redirected pages (the `-texas-` variants deleted in Phase 2)
       - All 9 service hub pages present (added in Phase 6)
       - All 5 city hub pages added in Phase 7 present
       - Trailing slashes on all URLs

    4. Rebuild the site to include the updated sitemap in out/:
       ```
       npm run build
       ```

    5. Verify the sitemap in out/ matches:
       ```
       diff public/sitemap.xml out/sitemap.xml
       ```
       They should be identical (static export copies public/ to out/).

    6. Run the 4 validation scripts again to confirm rebuild didn't break anything:
       ```
       node scripts/validate-schema.js
       node scripts/audit-meta.js
       ```
       (Schema and meta are sufficient — images and alt text won't change from a sitemap update.)
  </action>
  <verify>
    ```
    node generate-sitemap.js | grep "routes"
    diff public/sitemap.xml out/sitemap.xml
    ```
    Sitemap route count matches page.tsx count. Sitemap in out/ matches public/.
  </verify>
  <done>public/sitemap.xml has all 216+ URLs with differentiated priorities, site rebuilt with updated sitemap, no regressions in validation scripts.</done>
</task>

</tasks>

<verification>
1. `generate-sitemap.js` produces correct priority tiers
2. Sitemap URL count matches page.tsx file count
3. No stale/deleted URLs in sitemap
4. All new hub pages (Phase 6, 7) present in sitemap
5. Site rebuilds successfully with updated sitemap
6. Validation scripts still pass after rebuild
</verification>

<success_criteria>
- Sitemap contains all pages with no missing or extra URLs
- Hub pages have priority 0.8-0.9, spoke pages 0.7, content pages 0.5
- Site builds clean with updated sitemap included in out/
</success_criteria>

<output>
After completion, create `.planning/phases/10-technical-validation/10-02-SUMMARY.md`
</output>
