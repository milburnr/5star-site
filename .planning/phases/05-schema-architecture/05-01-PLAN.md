---
phase: 05-schema-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/schema-templates.ts
  - package.json
  - app/**/page.tsx
autonomous: true

must_haves:
  truths:
    - "Every page's LocalBusiness schema uses @type [\"LocalBusiness\", \"RoofingContractor\"] instead of string \"LocalBusiness\""
    - "Every schema name field reads \"5 Star Roofing\" (not \"5 Star Commercial Roofing\")"
    - "schema-dts is installed as a dev dependency"
    - "lib/schema-templates.ts generates the correct @type array and entity name"
  artifacts:
    - path: "lib/schema-templates.ts"
      provides: "Updated LocalBusiness schema generator with RoofingContractor type"
      contains: "RoofingContractor"
    - path: "package.json"
      provides: "schema-dts dev dependency"
      contains: "schema-dts"
  key_links:
    - from: "app/**/page.tsx"
      to: "schema @type field"
      via: "inline JSON-LD script"
      pattern: '\\["LocalBusiness", "RoofingContractor"\\]'
---

<objective>
Upgrade all 174 LocalBusiness schemas to use ["LocalBusiness", "RoofingContractor"] @type array, fix ~370 entity name occurrences from "5 Star Commercial Roofing" to "5 Star Roofing" in schema blocks, install schema-dts, and update lib/schema-templates.ts.

Purpose: SCHM-01 (RoofingContractor type), SCHM-07 (schema-dts), plus entity name consistency in schema name fields.
Output: All page schemas upgraded, schema-templates.ts corrected, schema-dts installed.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/brand/entity-guidelines.md
@.planning/phases/05-schema-architecture/05-RESEARCH.md
@lib/schema-templates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install schema-dts and upgrade schema-templates.ts</name>
  <files>package.json, lib/schema-templates.ts</files>
  <action>
1. Install schema-dts as a dev dependency:
   ```bash
   cd sites/5-star-roofing && npm install --save-dev schema-dts
   ```

2. Update `lib/schema-templates.ts`:
   - Add `import type { WithContext, LocalBusiness, FAQPage } from 'schema-dts';` at top
   - Change `"@type": "LocalBusiness"` to `"@type": ["LocalBusiness", "RoofingContractor"]`
   - Change `"name": "5 Star Commercial Roofing"` to `"name": "5 Star Roofing"`
   - The function signatures and return structure stay the same
   - Note: schema-dts doesn't natively support multi-type arrays, so keep the return type as plain object (not typed as WithContext<LocalBusiness>) — the import proves we have the types available for future use
  </action>
  <verify>
   - `grep "schema-dts" package.json` shows the dependency
   - `grep "RoofingContractor" lib/schema-templates.ts` returns a match
   - `grep '"5 Star Commercial Roofing"' lib/schema-templates.ts` returns NO matches
  </verify>
  <done>schema-dts installed, schema-templates.ts generates correct @type array and entity name</done>
</task>

<task type="auto">
  <name>Task 2: Mass update @type and entity name across all page.tsx files</name>
  <files>app/**/page.tsx</files>
  <action>
Write a Node.js script (scripts/upgrade-schema.js) that:

1. Finds all `app/**/page.tsx` files using `glob`
2. For each file, applies TWO replacements:
   - Replace `"@type": "LocalBusiness"` with `"@type": ["LocalBusiness", "RoofingContractor"]` (174 occurrences expected)
   - Replace `"name": "5 Star Commercial Roofing"` with `"name": "5 Star Roofing"` ONLY in schema context (inside JSON.stringify blocks or const schema objects). To be safe, replace ALL occurrences of the exact string `"5 Star Commercial Roofing"` in schema name fields. The pattern is: `"name": "5 Star Commercial Roofing"` → `"name": "5 Star Roofing"`
3. Support `--dry-run` flag to preview changes without writing
4. Print count of files changed and total replacements

Run with --dry-run first to verify counts, then run for real.

IMPORTANT: The homepage (`app/page.tsx`) already uses `"@type": "RoofingContractor"` (not "LocalBusiness") — the script should ALSO change `"@type": "RoofingContractor"` (standalone string, not in array) to `"@type": ["LocalBusiness", "RoofingContractor"]` for consistency.

IMPORTANT: The about page (`app/about/page.tsx`) uses `"@type": "Organization"` — do NOT change Organization to the array form. Only change "LocalBusiness" and standalone "RoofingContractor" @type values.

IMPORTANT: Some pages use `"5 Star Commercial Roofing"` in non-schema contexts (blog, gallery page names per 01-02 decision). The entity-name replacement should target ALL `"name": "5 Star Commercial Roofing"` strings because Phase 01-02 already preserved the blog/gallery-specific names. The remaining ~370 occurrences are all in LocalBusiness/Service schema contexts and should all become "5 Star Roofing".

After running, verify:
```bash
grep -r '"@type": "LocalBusiness"' app/ | wc -l  # Should be 0
grep -r '"name": "5 Star Commercial Roofing"' app/ | wc -l  # Should be 0 or very low (blog/gallery only)
```
  </action>
  <verify>
   - `grep -r '"@type": "LocalBusiness"' app/ | wc -l` returns 0
   - `grep -r '"@type": \["LocalBusiness"' app/ | wc -l` returns 174+
   - `grep -r '"name": "5 Star Commercial Roofing"' app/ | wc -l` returns 0 (or only blog/gallery exceptions)
   - `npm run build` completes with zero errors
  </verify>
  <done>All 174+ pages have @type array, all schema name fields use canonical "5 Star Roofing", build succeeds</done>
</task>

</tasks>

<verification>
1. `grep -r '"@type": "LocalBusiness"' app/ | wc -l` = 0 (no standalone LocalBusiness string)
2. `grep -r '"@type": \["LocalBusiness", "RoofingContractor"\]' app/ | wc -l` >= 174
3. `grep -r '"name": "5 Star Commercial Roofing"' app/ | wc -l` = 0 (or blog/gallery only)
4. `grep "schema-dts" package.json` shows installed
5. `npm run build` succeeds
</verification>

<success_criteria>
- Every LocalBusiness schema in app/ uses ["LocalBusiness", "RoofingContractor"] @type
- Every schema name field uses "5 Star Roofing" canonical name
- schema-dts is installed as devDependency
- lib/schema-templates.ts is updated
- Site builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/05-schema-architecture/05-01-SUMMARY.md`
</output>
