---
phase: 05-schema-architecture
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/page.tsx
  - app/about/page.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage contains a WebSite entity schema with sameAs links"
    - "About page contains a Person schema for the business owner"
    - "WebSite schema has correct name, url, and publisher @id"
    - "Both pages build without errors"
  artifacts:
    - path: "app/page.tsx"
      provides: "WebSite JSON-LD schema block"
      contains: "WebSite"
    - path: "app/about/page.tsx"
      provides: "Person JSON-LD schema block"
      contains: "Person"
  key_links:
    - from: "app/page.tsx"
      to: "WebSite schema"
      via: "script type=application/ld+json"
      pattern: '"@type": "WebSite"'
    - from: "app/about/page.tsx"
      to: "Person schema"
      via: "script type=application/ld+json"
      pattern: '"@type": "Person"'
---

<objective>
Add WebSite entity schema to homepage (SCHM-02) and Person schema to about page (SCHM-03). These are new schema blocks added alongside existing ones.

Purpose: Strengthen entity signals for Google's knowledge graph — WebSite schema associates the domain with the business entity, Person schema identifies the business owner.
Output: Two pages updated with additional JSON-LD script blocks.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/brand/entity-guidelines.md
@.planning/phases/05-schema-architecture/05-RESEARCH.md
@app/page.tsx
@app/about/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WebSite schema to homepage</name>
  <files>app/page.tsx</files>
  <action>
Add a new `<script type="application/ld+json">` block to the homepage (`app/page.tsx`) containing a WebSite schema. Place it AFTER the existing RoofingContractor schema block (do not replace the existing one — add a second JSON-LD script tag).

WebSite schema content:
```json
{
  "@context": "https://schema.org",
  "@type": "WebSite",
  "name": "5 Star Roofing",
  "url": "https://5starroofingpros.com",
  "publisher": {
    "@type": "Organization",
    "name": "5 Star Roofing",
    "@id": "https://5starroofingpros.com/#organization"
  },
  "sameAs": [
    "https://share.google/fl2pyMYIGwDQTd5KX",
    "https://www.facebook.com/5starroofingpros"
  ]
}
```

For the `sameAs` links:
- Check if the homepage already has sameAs URLs in its existing schema — reuse those exact URLs
- The existing schema at line ~739 of app/page.tsx may already contain social links — extract them
- If no sameAs found in existing schema, use the `BUSINESS_INFO.social.googleBusiness` value from constants.ts: `"https://share.google/fl2pyMYIGwDQTd5KX"`
- Add a Facebook URL if one exists in the codebase (grep for "facebook" in app/)

Use `dangerouslySetInnerHTML={{ __html: JSON.stringify(websiteSchema) }}` pattern matching the existing schema blocks on the page.
  </action>
  <verify>
   - `grep "WebSite" app/page.tsx` returns a match
   - `grep "sameAs" app/page.tsx` returns a match
   - `grep "YOUR_CID" app/page.tsx` returns NO matches (no placeholders remaining)
   - `npm run build` succeeds (page compiles without JSX errors)
  </verify>
  <done>Homepage has WebSite JSON-LD schema with name, url, publisher, and sameAs links</done>
</task>

<task type="auto">
  <name>Task 2: Add Person schema to about page</name>
  <files>app/about/page.tsx</files>
  <action>
Add a new `<script type="application/ld+json">` block to the about page (`app/about/page.tsx`) containing a Person schema. Place it AFTER the existing Organization/BreadcrumbList schema blocks.

Person schema content:
```json
{
  "@context": "https://schema.org",
  "@type": "Person",
  "name": "Owner",
  "jobTitle": "Owner",
  "worksFor": {
    "@type": "Organization",
    "name": "5 Star Roofing",
    "@id": "https://5starroofingpros.com/#organization"
  },
  "knowsAbout": ["Roofing", "Commercial Roofing", "Residential Roofing", "Storm Damage Repair"]
}
```

NOTE: The owner's actual name is not known. Use "Owner" as a placeholder name. The `worksFor` reference links to the Organization entity. If the about page content mentions the owner by name, use that name instead of "Owner".

Read the about page first to check if there is a name mentioned in the content (look for phrases like "founded by", "owner", "led by", etc.).

Also while editing the about page: verify the Organization schema `"name"` is `"5 Star Roofing"` (plan 05-01 should have already fixed this in wave 1). If it still reads `"5 Star Commercial Roofing"`, fix it.

Use `dangerouslySetInnerHTML={{ __html: JSON.stringify(personSchema) }}` pattern.
  </action>
  <verify>
   - `grep "Person" app/about/page.tsx` returns a match
   - `grep "worksFor" app/about/page.tsx` returns a match
   - `npm run build` succeeds
  </verify>
  <done>About page has Person JSON-LD schema with worksFor linking to Organization entity</done>
</task>

</tasks>

<verification>
1. `grep -c "WebSite" app/page.tsx` >= 1
2. `grep -c "Person" app/about/page.tsx` >= 1
3. `grep -c "sameAs" app/page.tsx` >= 1
4. `npm run build` succeeds
5. Spot-check: built `out/index.html` contains "WebSite" in a JSON-LD block
6. Spot-check: built `out/about/index.html` contains "Person" in a JSON-LD block
</verification>

<success_criteria>
- Homepage has WebSite entity schema with sameAs links
- About page has Person schema linked to Organization via worksFor
- Both pages build successfully
- No existing schema blocks broken or removed
</success_criteria>

<output>
After completion, create `.planning/phases/05-schema-architecture/05-02-SUMMARY.md`
</output>
