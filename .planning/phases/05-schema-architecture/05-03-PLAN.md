---
phase: 05-schema-architecture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/**/page.tsx (FAQ gap pages ~34 files + Service schema gap pages ~14 files)
  - scripts/schema-audit-report.txt
autonomous: true

must_haves:
  truths:
    - "Every page with FAQ accordion content also has FAQPage schema in its JSON-LD"
    - "No pages have FAQPage schema without corresponding FAQ content"
    - "Every service-specific page has a Service schema block"
    - "Breadcrumb and Service schema coverage is documented in a durable artifact"
  artifacts:
    - path: "app/**/page.tsx"
      provides: "FAQPage schema added to gap pages, Service schema added to gap pages"
      contains: "FAQPage"
    - path: "scripts/schema-audit-report.txt"
      provides: "Durable coverage report for FAQ, Breadcrumb, and Service schema"
      contains: "Coverage Report"
  key_links:
    - from: "app/**/page.tsx (FAQ gap pages)"
      to: "FAQPage JSON-LD"
      via: "script type=application/ld+json with AccordionTrigger content mirrored"
      pattern: '"@type": "FAQPage"'
    - from: "app/**/page.tsx (service gap pages)"
      to: "Service JSON-LD"
      via: "script type=application/ld+json"
      pattern: '"@type": "Service"'
---

<objective>
Audit and fix FAQ schema coverage across all pages. Currently 158 pages have FAQ accordion UI content but only 124 have FAQPage schema — ~34 pages are missing FAQ schema. Add FAQPage schema to every page that has FAQ content.

Purpose: SCHM-04 (FAQ schema on every page with FAQ content), SCHM-05 (Breadcrumb verification), SCHM-06 (Service schema on every service page).
Output: ~34 pages gain FAQPage schema, ~14 pages gain Service schema, durable coverage report at scripts/schema-audit-report.txt.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-schema-architecture/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify FAQ gap pages and add FAQPage schema</name>
  <files>app/**/page.tsx (~34 files)</files>
  <action>
Write a Node.js script (scripts/fix-faq-schema-gaps.js) that:

1. **Identify gap pages:** Find all `app/**/page.tsx` files that contain `AccordionTrigger` (FAQ UI) but do NOT contain `"FAQPage"` (schema). Print the list of gap pages.

2. **Extract FAQ content:** For each gap page, parse the AccordionTrigger/AccordionContent pairs to extract question/answer text. The pattern in these files is:
   ```tsx
   <AccordionTrigger>Question text here?</AccordionTrigger>
   <AccordionContent>Answer text here.</AccordionContent>
   ```
   Use regex to extract Q&A pairs. Clean HTML tags from the extracted text (strip `<p>`, `<strong>`, etc. leaving plain text).

3. **Generate FAQPage schema:** For each gap page, generate a FAQPage JSON-LD block:
   ```json
   {
     "@context": "https://schema.org",
     "@type": "FAQPage",
     "mainEntity": [
       {
         "@type": "Question",
         "name": "Question text?",
         "acceptedAnswer": {
           "@type": "Answer",
           "text": "Answer text."
         }
       }
     ]
   }
   ```

4. **Insert into page:** Add a new `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqSchema) }} />` block. Insert it right BEFORE the closing `</Head>` or right after the last existing `application/ld+json` script tag in the file. Follow the pattern used by existing pages that have FAQ schema.

5. **Support --dry-run** to preview which pages would be modified and how many Q&A pairs extracted.

Run with --dry-run first, then for real. Print summary: N pages fixed, M total FAQ items added.

IMPORTANT: Some AccordionTrigger usage may not be FAQ content (e.g., service detail accordions). The script should log which pages and Q&A pairs it extracts for manual verification. If a page has fewer than 2 Q&A pairs or the content looks non-FAQ (no question marks in triggers), skip it and log a warning.
  </action>
  <verify>
   - `grep -rl 'AccordionTrigger' app/ | wc -l` = `grep -rl 'FAQPage' app/ | wc -l` (or very close — all FAQ pages have schema)
   - Spot-check 3 previously-gap pages: they now have FAQPage schema with correct Q&A text
   - `npm run build` succeeds
  </verify>
  <done>All pages with FAQ accordion content now have corresponding FAQPage schema. Gap reduced from ~34 to 0.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Service schema gaps and write durable coverage report</name>
  <files>app/**/page.tsx (~14 service gap files), scripts/schema-audit-report.txt</files>
  <action>
Write a Node.js script (scripts/fix-service-schema-gaps.js) that:

1. **Identify Service schema gap pages:** Find all `app/**/page.tsx` files that represent service-specific pages (location x service pages like `app/roof-repair-amarillo/page.tsx`) but do NOT contain `"@type": "Service"`. These are pages whose sibling pages (same service, different city) DO have Service schema.

   Heuristic: A page is a service page if its directory name matches the pattern `{service}-{city}` or `{service}-{city}-tx` AND sibling directories for the same service pattern have `"@type": "Service"` in their page.tsx.

2. **Add Service schema** to each gap page. Generate from the page content:
   ```json
   {
     "@context": "https://schema.org",
     "@type": "Service",
     "name": "[Service Name from page title/h1]",
     "provider": {
       "@type": "LocalBusiness",
       "name": "5 Star Roofing",
       "@id": "https://5starroofingpros.com/#organization"
     },
     "areaServed": {
       "@type": "City",
       "name": "[City Name]"
     }
   }
   ```
   Insert as a new `<script type="application/ld+json">` block following the existing schema blocks.

3. **Support --dry-run** to preview which pages would be modified.

Run with --dry-run first, then for real.

4. **Breadcrumb audit** (no fixes needed — component auto-generates schema):
   ```bash
   grep -rl 'Breadcrumb' app/ --include="page.tsx" | wc -l
   ```
   Verify ~201 pages. Homepage intentionally excluded.

5. **Write durable coverage report** to `scripts/schema-audit-report.txt`:
   ```
   Schema Coverage Report
   Generated: [date]
   ======================

   FAQ Schema:
     Pages with FAQ content (AccordionTrigger): NNN
     Pages with FAQPage schema: NNN
     Gaps remaining: N

   Service Schema:
     Service pages identified: NNN
     Pages with Service schema: NNN
     Gaps fixed this run: N
     Gaps remaining: N

   Breadcrumb Schema:
     Pages with Breadcrumb component: NNN
     Homepage excluded (correct): yes
     Coverage: complete / N gaps

   LocalBusiness Schema:
     Pages with LocalBusiness: NNN
     All use @type array: yes/no
   ```

IMPORTANT: Do NOT just print to stdout. The report MUST be written to `scripts/schema-audit-report.txt` as a durable artifact.
  </action>
  <verify>
   - `test -f scripts/schema-audit-report.txt` — file exists
   - `grep "Gaps remaining: 0" scripts/schema-audit-report.txt` — Service gaps closed (or documented exceptions)
   - Spot-check 2-3 previously-gap service pages: they now have `"@type": "Service"` schema
   - `npm run build` succeeds
  </verify>
  <done>Service schema gaps fixed (~14 pages). Durable coverage report written to scripts/schema-audit-report.txt. Breadcrumb coverage verified. All schema types documented.</done>
</task>

</tasks>

<verification>
1. FAQ gap count reduced to 0 (or near-0 if some accordions are non-FAQ)
2. Service schema gap count reduced to 0 (all service-specific pages have Service schema)
3. `npm run build` succeeds after FAQ and Service schema additions
4. `scripts/schema-audit-report.txt` exists with complete coverage data
5. No existing schema blocks broken
</verification>

<success_criteria>
- Every page with FAQ accordion content has FAQPage schema
- Every service-specific page has Service schema (absence is ERROR, not WARNING)
- Breadcrumb coverage verified (~201 pages, homepage excluded)
- Durable coverage report at scripts/schema-audit-report.txt
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-schema-architecture/05-03-SUMMARY.md`
</output>
