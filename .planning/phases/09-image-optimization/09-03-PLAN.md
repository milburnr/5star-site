---
phase: 09-image-optimization
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/hail-damage-repair/page.tsx
  - app/roof-repair/page.tsx
  - app/roof-replacement/page.tsx
  - app/storm-damage-repair/page.tsx
  - app/metal-roofing/page.tsx
  - app/wind-damage-repair/page.tsx
  - app/tpo-roofing/page.tsx
  - app/roof-inspections/page.tsx
  - app/asphalt-shingle-roofing/page.tsx
autonomous: true

must_haves:
  truths:
    - "All 9 service hub pages have at least 1 inline contextual image relevant to the service topic"
    - "Each inline image has keyword-rich alt text between 40-125 characters"
    - "No inline image duplicates the hero background image on the same page"
    - "Images are contextually placed near content that references the material or damage type they depict"
    - "All new image URLs are verified R2 URLs returning HTTP 200"
  artifacts:
    - path: "app/hail-damage-repair/page.tsx"
      provides: "Hub page with inline hail damage image"
      contains: "<img"
    - path: "app/roof-repair/page.tsx"
      provides: "Hub page with inline roof repair image"
      contains: "<img"
    - path: "app/metal-roofing/page.tsx"
      provides: "Hub page with inline metal roofing image"
      contains: "<img"
    - path: "app/tpo-roofing/page.tsx"
      provides: "Hub page with inline TPO image"
      contains: "<img"
    - path: "app/asphalt-shingle-roofing/page.tsx"
      provides: "Hub page with inline shingle image"
      contains: "<img"
  key_links:
    - from: "app/*/page.tsx (hub pages)"
      to: "R2 CDN images"
      via: "img src attribute"
      pattern: "pub-797574ea9b1b4ccda73d4f6afb5d90d5\\.r2\\.dev"
---

<objective>
Add contextual inline images to the 9 service hub pages where content discusses specific materials, damage types, and completed work but currently shows only a hero background.

Purpose: Success criteria #5 requires inline images of roofing materials, hail damage, and completed work to appear where content references them. The 9 hub pages (created in Phase 6) are the highest-value pages for this treatment -- they are service authority pages with substantial content discussing specific materials and processes, but have zero inline images. Adding even 1-2 well-placed images per hub significantly improves content quality and search presence (images appear in Google Image results and enrich the main SERP listing).

Output: 9 hub pages with 1-2 contextual inline images each, all from R2, all with keyword-rich alt text.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-image-optimization/09-RESEARCH.md
@.planning/phases/09-image-optimization/09-01-SUMMARY.md

Hub page references (created in Phase 6):
@app/hail-damage-repair/page.tsx
@app/metal-roofing/page.tsx
@app/tpo-roofing/page.tsx

R2 image inventory (from research):
- `images/completed/` — completed project photos (amarillo, channing, dumas, morton, odessa, perryton, stinnett, wichita-falls, san-angelo)
- `images/hail-damage/` — hail damage documentation (amarillo, borger, canyon, levelland, lubbock, midland, odessa, pampa, perryton, san-angelo)
- `images/commercial/` — commercial projects (amarillo, pampa, plainview)
- `images/shingle/` — residential shingle work (amarillo, borger, midland, odessa, perryton)
- `images/metal/` — metal roofing (amarillo, channing, memphis, odessa, sanderson, wichita-falls)
- `images/heroes/` — hero backgrounds (commercial, residential)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contextual inline images to 9 service hub pages</name>
  <files>
    app/hail-damage-repair/page.tsx
    app/roof-repair/page.tsx
    app/roof-replacement/page.tsx
    app/storm-damage-repair/page.tsx
    app/metal-roofing/page.tsx
    app/wind-damage-repair/page.tsx
    app/tpo-roofing/page.tsx
    app/roof-inspections/page.tsx
    app/asphalt-shingle-roofing/page.tsx
  </files>
  <action>
For each hub page, add 1-2 inline `<img>` tags placed within the content body, near text that discusses the specific material or damage type the image depicts. Do NOT place images in the hero section or FAQ section -- place them in the main content area between descriptive paragraphs.

**Image HTML pattern (consistent with existing site pattern):**
```tsx
<div className="my-8 rounded-lg overflow-hidden shadow-lg">
  <img
    src="https://pub-797574ea9b1b4ccda73d4f6afb5d90d5.r2.dev/images/{category}/{filename}"
    alt="{descriptive alt text 40-125 chars}"
    className="w-full h-64 object-cover"
    loading="lazy"
  />
</div>
```

Use `my-8` for vertical spacing, `rounded-lg overflow-hidden shadow-lg` for visual consistency with existing card patterns, `h-64` for consistent height, `loading="lazy"` for performance.

**Image assignments per hub page:**

| Hub Page | Image 1 | Image 2 (if applicable) |
|----------|---------|------------------------|
| **hail-damage-repair** | `hail-damage/hail-damage-amarillo-16-1280w.webp` (place near "signs of hail damage" content) | `hail-damage/hail-damage-borger-12-1280w.webp` (place near repair process content) |
| **roof-repair** | `completed/completed-amarillo-18-1280w.jpg` (place near repair types content) | `hail-damage/hail-damage-midland-15-1280w.webp` (place near "when to repair" content) |
| **roof-replacement** | `completed/completed-amarillo-7-1280w.jpg` (place near replacement process content) | `shingle/shingle-amarillo-12-1280w.jpg` (place near materials section) |
| **storm-damage-repair** | `hail-damage/hail-damage-canyon-1-1280w.webp` (place near storm damage signs content) | `completed/completed-perryton-22-1280w.jpg` (place near repair process) |
| **metal-roofing** | `metal/metal-amarillo-40-1280w.webp` (place near benefits/features content) | `metal/metal-sanderson-22-1280w.webp` (place near installation process) |
| **wind-damage-repair** | `hail-damage/hail-damage-perryton-7-1280w.webp` (place near damage signs content) | `completed/completed-stinnett-23-1280w.jpg` (place near repair section) |
| **tpo-roofing** | `commercial/commercial-pampa-1-1280w.jpg` (place near TPO benefits content) | `commercial/commercial-amarillo-14-1280w.jpg` (place near installation process) |
| **roof-inspections** | `completed/completed-amarillo-3-1280w.jpg` (place near inspection process content) | -- (1 image sufficient for this page) |
| **asphalt-shingle-roofing** | `shingle/shingle-midland-1-1280w.jpg` (place near shingle types content) | `shingle/shingle-perryton-3-1280w.jpg` (place near installation section) |

**Before inserting each image:**
1. Verify the R2 URL returns HTTP 200: `curl -sI "https://pub-797574ea9b1b4ccda73d4f6afb5d90d5.r2.dev/images/{path}" | head -1`
2. Check the page's hero backgroundImage URL -- the inline image MUST be a different URL to avoid same-page duplication
3. If a URL 404s, substitute another image from the same R2 category

**Alt text for each image must:**
- Describe what the image shows (completed roof, damage close-up, installation in progress)
- Include the hub service keyword (hail damage, metal roofing, TPO, etc.)
- Include a location reference ("Texas Panhandle", "West Texas", or a specific city from the filename)
- Be 40-125 characters
- NOT duplicate any other alt text on the same page

**Placement rules:**
- Place image after a paragraph that discusses the topic the image shows
- Do NOT place images inside grid layouts or between FAQ items
- Do NOT add images to the hero section (already has background image)
- Place the image div at the same indentation level as surrounding content divs
  </action>
  <verify>
1. Each of the 9 hub pages contains at least 1 `<img` tag (beyond any existing ones): `for f in hail-damage-repair roof-repair roof-replacement storm-damage-repair metal-roofing wind-damage-repair tpo-roofing roof-inspections asphalt-shingle-roofing; do echo "$f: $(grep -c '<img' app/$f/page.tsx)"; done`
2. All new image URLs return HTTP 200 (spot check 5)
3. No inline image URL matches the hero backgroundImage URL on the same page
4. `npm run build` succeeds
5. `node scripts/audit-images.js` still passes (zero broken URLs)
6. `node scripts/audit-alt-text.js` passes (all new alts meet quality criteria)
  </verify>
  <done>All 9 service hub pages have 1-2 contextual inline images from R2, placed near relevant content, with descriptive keyword-rich alt text. No same-page duplicates. Build succeeds. Both audit scripts pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `node scripts/audit-images.js` exits 0 — zero broken URLs, 100% R2 compliance
3. `node scripts/audit-alt-text.js` exits 0 — all alt text meets quality criteria
4. Each hub page has at least 1 inline image tag
5. No inline image duplicates hero background on same page
6. Spot check: open 3 hub pages in browser, confirm images render and are contextually relevant
</verification>

<success_criteria>
- All 9 service hub pages have inline images showing relevant roofing materials, damage, or completed work
- Images placed near content that references what they depict
- All image URLs are R2 and return HTTP 200
- Alt text is descriptive, keyword-rich, 40-125 characters
- No duplication with hero background images
- Both audit scripts pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-image-optimization/09-03-SUMMARY.md`
</output>
