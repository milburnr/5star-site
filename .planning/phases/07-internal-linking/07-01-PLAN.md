---
phase: 07-internal-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/InternalLinks.tsx
autonomous: true

must_haves:
  truths:
    - "InternalLinks component renders a hub uplinks section with links to service hub and city hub"
    - "No phantom emergency-roof-repair links are generated"
    - "Cross-city and cross-service links render correctly for all 14 main cities"
  artifacts:
    - path: "components/InternalLinks.tsx"
      provides: "Hub uplinks, cross-city, cross-service internal links"
      contains: "hubUrl"
  key_links:
    - from: "components/InternalLinks.tsx"
      to: "service hub pages"
      via: "serviceData[x].hubUrl"
      pattern: "hubUrl.*href"
    - from: "components/InternalLinks.tsx"
      to: "city hub pages"
      via: "cityData[x].hubUrl"
      pattern: "hubUrl.*href"
---

<objective>
Rebuild the InternalLinks component to add hub uplinks (spoke-to-hub links), remove the phantom emergency-roof-repair service, and add city hub URL mappings.

Purpose: This component is the foundation for all internal linking in Phase 7. It must be correct before 147 spoke pages start rendering it.
Output: Updated InternalLinks.tsx with 3-section layout (hub uplinks + cross-city + cross-service)
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-internal-linking/07-RESEARCH.md
@components/InternalLinks.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild InternalLinks component with hub uplinks and data fixes</name>
  <files>components/InternalLinks.tsx</files>
  <action>
Modify the InternalLinks component with the following changes:

**1. Add `hubUrl` to CityData interface and cityData entries:**
```typescript
interface CityData {
  name: string;
  slug: string;
  nearbyCities: string[];
  hubUrl?: string;  // Optional -- 5 cities won't have this yet
}
```

Add hubUrl to each city that has a hub page:
- amarillo: '/amarillo-texas-roofing/'
- canyon: '/canyon-texas-roofing/'
- borger: '/borger-texas-roofing/'
- dumas: '/dumas-texas-roofing/'
- pampa: '/pampa-texas-roofing/'
- perryton: '/perryton-texas-roofing/'
- lubbock: '/lubbock-tx-roofing/'
- midland: '/midland-tx-roofing/'
- odessa: '/odessa-tx-roofing/'
- levelland: NO hubUrl (will be added in plan 02)
- big-spring: NO hubUrl (will be added in plan 02)
- snyder: NO hubUrl (will be added in plan 02)
- monahans: NO hubUrl (will be added in plan 02)
- andrews: NO hubUrl (will be added in plan 02)

**2. Add `hubUrl` to ServiceData interface and serviceData entries:**
```typescript
interface ServiceData {
  name: string;
  slug: string;
  hubUrl: string;
}
```

Add hubUrl to each service:
- residential-roofing: '/residential-roofing/'
- commercial-roofing: '/commercial-roofing/'
- hail-damage-repair: '/hail-damage-repair/'
- storm-damage-repair: '/storm-damage-repair/'
- wind-damage-repair: '/wind-damage-repair/'
- roof-replacement: '/roof-replacement/'
- roof-repair: '/roof-repair/'
- roof-inspections: '/roof-inspections/'
- tpo-roofing: '/tpo-roofing/'
- metal-roofing: '/metal-roofing/'
- asphalt-shingle-roofing: '/asphalt-shingle-roofing/'

**3. REMOVE `emergency-roof-repair` from serviceData entirely.** No pages exist for this service -- it generates 404 links.

**4. Add hub uplinks section to the JSX.** Before the existing two-column grid, add a new section at the top:

```tsx
{/* Hub Uplinks */}
<div className="mb-6 flex flex-wrap gap-3">
  <a
    href={serviceData[currentService]?.hubUrl}
    className="inline-flex items-center gap-2 px-4 py-2 bg-brand-brown text-white rounded-lg font-semibold hover:bg-brand-brown/90 transition-colors"
  >
    <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M7 16l-4-4m0 0l4-4m-4 4h18" />
    </svg>
    All {serviceData[currentService]?.name} Services
  </a>
  {cityInfo.hubUrl && (
    <a
      href={cityInfo.hubUrl}
      className="inline-flex items-center gap-2 px-4 py-2 bg-brand-gold text-white rounded-lg font-semibold hover:bg-brand-gold-bright transition-colors"
    >
      <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M7 16l-4-4m0 0l4-4m-4 4h18" />
      </svg>
      All Roofing Services in {cityInfo.name}
    </a>
  )}
</div>
```

The city hub link is conditionally rendered only when `cityInfo.hubUrl` exists (handles the 5 cities that don't have hub pages yet).

Keep the existing two-column cross-city/cross-service grid below the hub uplinks. Do not change the styling of the existing sections.

**5. Update the section heading for clarity.** Change the outer section heading context if needed. The section already has a wrapping `<section>` with the amber gradient background -- keep that.
  </action>
  <verify>
Run `npx tsc --noEmit` from the site directory to confirm no TypeScript errors. Then grep the component for "emergency-roof-repair" (should find zero matches) and "hubUrl" (should find multiple matches).
  </verify>
  <done>
InternalLinks component renders 3 sections: hub uplinks (service hub + optional city hub), cross-city links, and cross-service links. No emergency-roof-repair in serviceData. All 9 existing city hub URLs mapped. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `grep "emergency-roof-repair" components/InternalLinks.tsx` returns nothing
3. `grep "hubUrl" components/InternalLinks.tsx` returns multiple matches
4. Component has exactly 11 services in serviceData (not 12)
5. Component has hubUrl for 9 of 14 cities
</verification>

<success_criteria>
InternalLinks component is ready for all spoke pages to render it. Hub uplinks work for cities with hub pages, gracefully omit city hub link for cities without. No phantom service generates broken links.
</success_criteria>

<output>
After completion, create `.planning/phases/07-internal-linking/07-01-SUMMARY.md`
</output>
