---
phase: 08-content-enrichment
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/wind-damage-repair-andrews/page.tsx
  - app/wind-damage-repair-big-spring/page.tsx
  - app/tpo-roofing-andrews/page.tsx
  - app/tpo-roofing-big-spring/page.tsx
  - app/tpo-roofing-monahans/page.tsx
  - app/roof-inspections-andrews/page.tsx
  - app/canyon-texas-roofing/page.tsx
  - app/borger-texas-roofing/page.tsx
  - app/pampa-texas-roofing/page.tsx
  - app/dumas-texas-roofing/page.tsx
  - app/amarillo-texas-roofing/page.tsx
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "All 6 spoke pages have visible AccordionItem FAQ sections with 6 items each"
    - "All 6 spoke pages have FAQPage JSON-LD schema matching visible FAQ content"
    - "All 5 city hub pages have FAQPage JSON-LD schema matching their existing FAQ content"
    - "Amarillo city hub has a visible FAQ section and FAQPage schema"
  artifacts:
    - path: "app/wind-damage-repair-andrews/page.tsx"
      provides: "FAQ section with AccordionItem + FAQPage schema"
      contains: "FAQPage"
    - path: "app/wind-damage-repair-big-spring/page.tsx"
      provides: "FAQ section with AccordionItem + FAQPage schema"
      contains: "FAQPage"
    - path: "app/tpo-roofing-andrews/page.tsx"
      provides: "FAQ section with AccordionItem + FAQPage schema"
      contains: "FAQPage"
    - path: "app/tpo-roofing-big-spring/page.tsx"
      provides: "FAQ section with AccordionItem + FAQPage schema"
      contains: "FAQPage"
    - path: "app/tpo-roofing-monahans/page.tsx"
      provides: "FAQ section with AccordionItem + FAQPage schema"
      contains: "FAQPage"
    - path: "app/roof-inspections-andrews/page.tsx"
      provides: "FAQ section with AccordionItem + FAQPage schema"
      contains: "FAQPage"
    - path: "app/canyon-texas-roofing/page.tsx"
      provides: "FAQPage JSON-LD schema"
      contains: "FAQPage"
    - path: "app/borger-texas-roofing/page.tsx"
      provides: "FAQPage JSON-LD schema"
      contains: "FAQPage"
    - path: "app/pampa-texas-roofing/page.tsx"
      provides: "FAQPage JSON-LD schema"
      contains: "FAQPage"
    - path: "app/dumas-texas-roofing/page.tsx"
      provides: "FAQPage JSON-LD schema"
      contains: "FAQPage"
    - path: "app/amarillo-texas-roofing/page.tsx"
      provides: "FAQ section + FAQPage schema"
      contains: "FAQPage"
  key_links:
    - from: "AccordionItem FAQ section"
      to: "FAQPage JSON-LD schema"
      via: "faqItems array used by both .map() renders"
      pattern: "FAQPage"
    - from: "div/h3 FAQ content (city hubs)"
      to: "FAQPage JSON-LD schema"
      via: "matching question text in schema mainEntity"
      pattern: "mainEntity"
---

<objective>
Close FAQ coverage gaps identified in 08-VERIFICATION.md Truth 2.

Purpose: 6 spoke pages import AccordionItem but never render FAQ sections (no visible FAQ, no schema). 4 city hubs have FAQ content in plain div/h3 format but lack FAQPage JSON-LD schema. Amarillo city hub has no FAQ section at all. This plan adds the missing FAQ sections and schema to all 11 pages.

Output: All 11 pages have both visible FAQ content and matching FAQPage JSON-LD schema.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/brand/content-voice.md
@.planning/phases/08-content-enrichment/08-VERIFICATION.md
@.planning/phases/08-content-enrichment/08-02-SUMMARY.md

Reference patterns:
@app/andrews-tx-roofing/page.tsx (faqItems array + .map() + FAQPage schema -- single source of truth pattern from 08-01)
@app/hail-damage-repair-hereford/page.tsx (AccordionItem FAQ section + FAQPage schema pattern from 08-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FAQ sections and schema to 6 spoke pages</name>
  <files>
app/wind-damage-repair-andrews/page.tsx
app/wind-damage-repair-big-spring/page.tsx
app/tpo-roofing-andrews/page.tsx
app/tpo-roofing-big-spring/page.tsx
app/tpo-roofing-monahans/page.tsx
app/roof-inspections-andrews/page.tsx
  </files>
  <action>
These 6 pages already import Accordion/AccordionContent/AccordionItem/AccordionTrigger but never use them. Add FAQ sections and FAQPage schema to each page.

Use the **faqItems array + .map() single source of truth pattern** (from app/andrews-tx-roofing/page.tsx, established in 08-01). This ensures FAQ content and schema always match:

1. **Define faqItems array** at the top of the component function (before the return statement):
   ```tsx
   const faqItems = [
     { question: "...", answer: "..." },
     // 5 more items (6 total)
   ];
   ```

2. **Add visible FAQ section** before the final CTA section, using the established styling:
   ```tsx
   <FadeIn>
     <section className="mb-16 bg-gradient-to-br from-blue-50 to-white p-8 md:p-12 rounded-3xl shadow-lg">
       <h2 className="text-3xl font-bold mb-8 text-center text-brand-brown">
         {Service} in {City} FAQs
       </h2>
       <Accordion type="single" collapsible className="max-w-4xl mx-auto">
         {faqItems.map((faq, index) => (
           <AccordionItem key={`faq-${index + 1}`} value={`faq-${index + 1}`} className="bg-white rounded-xl shadow-sm border border-gray-100 px-6 mb-4">
             <AccordionTrigger className="text-lg font-semibold text-brand-brown hover:text-brand-gold">
               {faq.question}
             </AccordionTrigger>
             <AccordionContent className="text-gray-700 leading-relaxed">
               {faq.answer}
             </AccordionContent>
           </AccordionItem>
         ))}
       </Accordion>
     </section>
   </FadeIn>
   ```

3. **Add FAQPage JSON-LD schema** as a separate script tag after the existing LocalBusiness schema:
   ```tsx
   <script
     type="application/ld+json"
     dangerouslySetInnerHTML={{
       __html: JSON.stringify({
         "@context": "https://schema.org",
         "@type": "FAQPage",
         "mainEntity": faqItems.map(faq => ({
           "@type": "Question",
           "name": faq.question,
           "acceptedAnswer": { "@type": "Answer", "text": faq.answer }
         }))
       })
     }}
   />
   ```

4. **Write service+city-specific FAQ questions** -- 6 unique items per page. No question may duplicate any existing FAQ on other pages. Each must reference both the specific service and the specific city conditions:

   **wind-damage-repair-andrews:** Andrews County wind patterns, Permian Basin dust storms, oil field building exposure, wind speed thresholds for insurance claims, FORTIFIED roofing for high-wind zones, temporary tarping timelines

   **wind-damage-repair-big-spring:** Howard County wind corridors, Big Spring Refinery industrial area wind loading, I-20 corridor weather, wind vs hail damage differentiation, commercial building wind codes, emergency response for wind events

   **tpo-roofing-andrews:** TPO membrane performance in Permian Basin heat (110F+), UV resistance for Andrews' 280+ sunny days, chemical resistance near oil operations, TPO vs PVC for Andrews climate, energy savings for commercial buildings, TPO warranty coverage

   **tpo-roofing-big-spring:** TPO for Big Spring commercial/industrial buildings, membrane thickness for Howard County hail, reflective roofing for West Texas sun, TPO installation timeline, Big Spring building code requirements, maintenance schedule for TPO

   **tpo-roofing-monahans:** TPO for Monahans sand exposure, membrane durability in Ward County desert climate, dust accumulation and cleaning, TPO for oil/gas facility roofing, heat-welded seam integrity in extreme temperatures, Monahans-specific warranty considerations

   **roof-inspections-andrews:** Inspection frequency for Andrews commercial buildings, what inspectors look for in Permian Basin conditions, pre-purchase roof inspection scope, insurance inspection documentation, hail damage assessment criteria, drone vs manual inspection methods

CRITICAL: No duplicate questions across any pages. Each question must be specific to its service type AND city.
  </action>
  <verify>
For each of the 6 pages, run:
- `grep -c "AccordionItem" app/{page}/page.tsx` -- should be 6 or more (6 FAQ items + potential import)
- `grep "FAQPage" app/{page}/page.tsx` -- should find FAQPage schema
- `grep "faqItems" app/{page}/page.tsx` -- should find the array definition and usage
  </verify>
  <done>All 6 spoke pages have visible Accordion FAQ sections with 6 items each and matching FAQPage JSON-LD schema using the faqItems single source of truth pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Add FAQPage schema to 4 city hubs and FAQ section + schema to Amarillo hub</name>
  <files>
app/canyon-texas-roofing/page.tsx
app/borger-texas-roofing/page.tsx
app/pampa-texas-roofing/page.tsx
app/dumas-texas-roofing/page.tsx
app/amarillo-texas-roofing/page.tsx
  </files>
  <action>
**For Canyon, Borger, Pampa, Dumas (4 pages with existing div/h3 FAQ content):**

These pages have FAQ items in plain div/h3 format (e.g., Canyon has 5-6 items with `<h3>` questions and `<p>` answers). They need FAQPage JSON-LD schema added to match their existing visible content.

1. **Read the existing FAQ questions and answers** from each page's div/h3 section.

2. **Add a FAQPage JSON-LD schema script tag** after the existing LocalBusiness schema. Extract the exact question text from the h3 elements and answer text from the p elements:
   ```tsx
   <script
     type="application/ld+json"
     dangerouslySetInnerHTML={{
       __html: JSON.stringify({
         "@context": "https://schema.org",
         "@type": "FAQPage",
         "mainEntity": [
           {
             "@type": "Question",
             "name": "exact h3 question text from page",
             "acceptedAnswer": { "@type": "Answer", "text": "exact p answer text from page" }
           },
           // ... one entry for each existing FAQ item
         ]
       })
     }}
   />
   ```

3. **Do NOT modify the visible FAQ sections.** The div/h3 format is already styled and working. Only add the JSON-LD schema.

4. Place the FAQPage schema script tag right after the existing LocalBusiness/RoofingContractor schema script tag (before the closing `</div>` or `</>` of the page).

**For Amarillo city hub (no FAQ section at all):**

1. **Add Accordion imports** at the top:
   ```tsx
   import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";
   ```

2. **Define faqItems array** with 6 Amarillo-specific roofing FAQ items:
   - Amarillo's position as primary service area (largest Panhandle city, 200k+ metro)
   - Hail season timing and frequency in Potter/Randall County
   - Insurance claim process for Amarillo homeowners
   - Residential vs commercial roofing needs in Amarillo neighborhoods
   - Amarillo building codes and permit requirements for roofing
   - Emergency roofing response time within Amarillo city limits

3. **Add visible Accordion FAQ section** before the final CTA, using the standard styling pattern (same as Task 1).

4. **Add FAQPage JSON-LD schema** using the faqItems.map() single source of truth pattern (same as Task 1).

CRITICAL: Amarillo FAQ questions must not duplicate any existing FAQ questions on other Amarillo service pages or the homepage.
  </action>
  <verify>
For each of the 5 city hub pages:
- `grep "FAQPage" app/{page}/page.tsx` -- must find FAQPage schema
- `grep "mainEntity" app/{page}/page.tsx` -- must find mainEntity array

For Amarillo specifically:
- `grep -c "AccordionItem" app/amarillo-texas-roofing/page.tsx` -- should be 6+ (FAQ items)
- `grep "faqItems" app/amarillo-texas-roofing/page.tsx` -- should find array

Final: `npm run build` -- must complete with zero errors.
  </verify>
  <done>All 4 city hubs (Canyon, Borger, Pampa, Dumas) have FAQPage JSON-LD schema matching their existing visible FAQ content. Amarillo city hub has both a new visible Accordion FAQ section and matching FAQPage schema. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. All 11 pages have FAQPage JSON-LD schema: `grep -rl "FAQPage" app/wind-damage-repair-andrews/ app/wind-damage-repair-big-spring/ app/tpo-roofing-andrews/ app/tpo-roofing-big-spring/ app/tpo-roofing-monahans/ app/roof-inspections-andrews/ app/canyon-texas-roofing/ app/borger-texas-roofing/ app/pampa-texas-roofing/ app/dumas-texas-roofing/ app/amarillo-texas-roofing/` -- should return 11 files
3. All 6 spoke pages + Amarillo have visible AccordionItem FAQ sections: `grep -c "AccordionItem" app/{page}/page.tsx` returns 6+ for each
4. No duplicate FAQ questions across the 11 pages (spot-check 3 pairs)
</verification>

<success_criteria>
- Truth 2 from 08-VERIFICATION.md fully resolved: all service-city pages have FAQ AccordionItem sections with FAQPage schema
- All 6 Group A spoke pages: visible Accordion FAQ + FAQPage schema (was: import only, no content)
- All 4 Group B city hubs: FAQPage schema added (was: div/h3 content, no schema)
- Amarillo city hub: visible FAQ section + FAQPage schema added (was: neither)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-enrichment/08-05-SUMMARY.md`
</output>
