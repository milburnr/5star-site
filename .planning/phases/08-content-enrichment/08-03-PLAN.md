---
phase: 08-content-enrichment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/roof-repair-*/page.tsx
  - app/roof-replacement-*/page.tsx
  - app/commercial-roofing-*/page.tsx
  - app/residential-roofing-*/page.tsx
  - app/storm-damage-repair-*/page.tsx
  - app/wind-damage-repair-*/page.tsx
  - app/roof-inspections-*/page.tsx
  - app/metal-roofing-*/page.tsx
  - app/tpo-roofing-*/page.tsx
  - app/asphalt-shingle-roofing-*/page.tsx
  - app/hail-damage-repair-*/page.tsx
  - app/roof-repair/page.tsx
  - app/roof-replacement/page.tsx
  - app/storm-damage-repair/page.tsx
  - app/metal-roofing/page.tsx
  - app/tpo-roofing/page.tsx
  - app/asphalt-shingle-roofing/page.tsx
  - app/roof-inspections/page.tsx
  - app/wind-damage-repair/page.tsx
  - app/hail-damage-repair/page.tsx
autonomous: true

must_haves:
  truths:
    - "Service page H2s are framed as homeowner questions (answer-first format)"
    - "Each H2 still contains the page's primary keyword from keyword-map.md"
    - "H2s are unique across sibling pages targeting the same service in different cities"
    - "No CSS, layout, or visual changes were made — content-only edits"
  artifacts:
    - path: "app/roof-repair-*/page.tsx"
      provides: "Service spoke pages with answer-first H2 structure"
      contains: "?"
  key_links:
    - from: "H2 text"
      to: "keyword-map.md primary keyword"
      via: "H2 contains the page's primary keyword terms"
      pattern: "\\?"
---

<objective>
Restructure H2 headings on all service pages from statement-style to answer-first question format, and add rich content elements where appropriate.

Purpose: CONT-05 requires answer-first H2 structure on service pages (H2s framed as homeowner questions). CONT-07 requires rich content elements (comparison tables, material specs, process steps). Current H2s like "Common Roof Repairs in Amarillo" become "What Are the Most Common Roof Repairs in Amarillo?" — targeting featured snippets and voice search.

Output: All service pages (hub + spoke) use question-format H2s with primary keywords preserved. Hub pages gain comparison tables and material specification content where appropriate.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/brand/content-voice.md
@.planning/brand/keyword-map.md
@.planning/phases/08-content-enrichment/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure H2s on service hub pages and add rich content elements</name>
  <files>
app/roof-repair/page.tsx
app/roof-replacement/page.tsx
app/storm-damage-repair/page.tsx
app/metal-roofing/page.tsx
app/tpo-roofing/page.tsx
app/asphalt-shingle-roofing/page.tsx
app/roof-inspections/page.tsx
app/wind-damage-repair/page.tsx
app/hail-damage-repair/page.tsx
  </files>
  <action>
**Step 1: Audit current H2s.** For each of the 9 service hub pages, read the file and list all H2 elements. Skip these H2s (do NOT restructure):
- FAQ section headers (e.g., "Frequently Asked Questions")
- CTA section headers (e.g., "Ready to Get Started?", "Contact Us Today")
- "Visit Our Location" or similar utility headers

**Step 2: Convert statement H2s to question H2s.** Transform each qualifying H2:

| Current Pattern | Target Pattern |
|----------------|----------------|
| "Common {Service} in {Region}" | "What Are the Most Common {Service} Issues in {Region}?" |
| "Why {City/Region} Needs {Service}" | "Why Does {City/Region} Need Professional {Service}?" |
| "Seasonal {Topic}" | "When Is the Best Time for {Service} in {Region}?" |
| "Our {Service} Process" | "How Does the {Service} Process Work?" |
| "Benefits of {Material}" | "What Are the Benefits of {Material} Roofing?" |
| "Signs You Need {Service}" | "How Do You Know You Need {Service}?" |

**CRITICAL:** Each new H2 must still contain the page's primary keyword terms from keyword-map.md. Example: if the page targets "roof repair west texas", the H2 must contain "roof repair."

**Step 3: Add rich content elements (CONT-07) to hub pages where appropriate:**

- **roof-replacement hub:** Add a material comparison section (asphalt shingles vs metal vs TPO/PVC vs BUR) with a simple HTML table or grid comparing lifespan, cost range, and best-use-case.
- **roof-repair hub:** Add a damage severity guide section (cosmetic vs functional vs critical) with descriptions of each level.
- **metal-roofing hub:** Add material specification section (standing seam vs corrugated vs stone-coated steel) with pros/cons.
- **asphalt-shingle-roofing hub:** Add shingle grade comparison (3-tab vs architectural vs designer/luxury) with lifespan and price ranges.

Use existing CSS classes only (grid layouts, feature-box, etc.). No new CSS.

**Step 4: Ensure CONT-06 coverage.** On roof-repair, roof-replacement, and commercial-roofing hub pages, verify that the content mentions all major roofing types: asphalt shingles, metal roofing, TPO, EPDM, PVC membrane, and built-up roofing (BUR). Add brief mentions if any type is missing.

Do NOT change className attributes, section structure, or animations. Only modify text content within existing elements, and add new content sections using established patterns.
  </action>
  <verify>
For each hub page: `grep -c "?" app/{service}/page.tsx` — count of question marks should be higher than before (H2s + FAQs).
Spot-check 3 hub pages: verify H2s are question-format and contain primary keyword.
Check roof-replacement hub has a comparison table/grid section.
  </verify>
  <done>All 9 service hub pages have answer-first H2s, primary keywords preserved, rich content elements added to at least 4 hubs (comparison tables, material specs, severity guides). CONT-05, CONT-06, CONT-07 addressed for hub pages.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure H2s on service spoke pages (batch processing)</name>
  <files>
app/roof-repair-*/page.tsx
app/roof-replacement-*/page.tsx
app/commercial-roofing-*/page.tsx
app/residential-roofing-*/page.tsx
app/storm-damage-repair-*/page.tsx
app/wind-damage-repair-*/page.tsx
app/roof-inspections-*/page.tsx
app/metal-roofing-*/page.tsx
app/tpo-roofing-*/page.tsx
app/asphalt-shingle-roofing-*/page.tsx
app/hail-damage-repair-*/page.tsx
  </files>
  <action>
Process all service spoke pages (~100-120 pages) to convert statement-style H2s to question format.

**Step 1: Identify pages needing H2 restructuring.** Run a grep to find all pages with H2s that do NOT end in "?":
```bash
grep -rl '<h2' app/*/page.tsx | while read f; do
  if grep -q '<h2' "$f" && ! grep -q '<h2.*\?' "$f"; then echo "$f"; fi
done
```
This gives the list of pages to process. Skip city hub pages (handled in 08-01) and service hub pages (handled in Task 1).

**Step 2: For each spoke page, convert H2s to questions.**

The transformation rules (same as Task 1):
- Statement → Question format
- Preserve primary keyword terms
- Skip FAQ headers, CTA headers, utility headers
- Make each H2 unique — avoid identical H2s across same-service spokes in different cities

**City differentiation in H2s:** When the same service has spokes in 14+ cities, differentiate H2s by referencing city-specific conditions:
- Amarillo: "How Does Amarillo's Hail Season Affect Your Roof?"
- Midland: "Why Do Midland's Oilfield Conditions Damage Roofs?"
- Lubbock: "What Makes Lubbock's Wind Patterns Unique for Roofing?"
- Canyon: "How Does Canyon's Palo Duro Canyon Microclimate Affect Roofs?"

**Step 3: Batch verify.** After all changes, run:
```bash
grep -rn '<h2' app/*/page.tsx | grep -v '?' | grep -v 'FAQ\|Contact\|Ready\|Visit\|Call\|Get Started\|Free\|Schedule'
```
This finds remaining statement-style H2s that should have been converted. Address any stragglers.

**IMPORTANT:** This is a high-volume task. Work systematically by service type (all roof-repair spokes, then all roof-replacement spokes, etc.) to maintain consistency. Do not attempt to add new content sections — only modify existing H2 text.
  </action>
  <verify>
Run: `grep -rn '<h2' app/*/page.tsx | grep -v '?' | grep -v -i 'faq\|contact\|ready\|visit\|call\|get started\|free\|schedule\|location\|serving\|also' | wc -l` — should be 0 or near 0 (only non-service pages remaining).
Run: `npm run build` — must complete with zero errors.
Spot-check 5 random spoke pages: H2s should be question format with primary keyword present.
  </verify>
  <done>All service spoke pages have answer-first H2s. Primary keywords preserved in every H2. No CSS or layout changes. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. Grep for statement-style H2s returns near-zero results (only non-service pages)
3. Spot-check 10 random service pages: all H2s are question format
4. Primary keywords from keyword-map.md are present in H2s
5. At least 4 hub pages have new rich content elements (tables, grids, or spec sections)
6. No CSS classes were added or modified
</verification>

<success_criteria>
- CONT-05 (answer-first H2 structure) satisfied across all service hub and spoke pages
- CONT-06 (roofing types coverage) verified on roof-repair, roof-replacement, commercial-roofing hubs
- CONT-07 (rich content elements) comparison tables/specs added to 4+ hub pages
- No visual/layout/CSS changes — content only
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-enrichment/08-03-SUMMARY.md`
</output>
