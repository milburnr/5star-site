# Image Intelligence E2E Run — Handoff Prompt

Paste this into Claude after clearing context:

---

Run the image intelligence E2E pipeline. The tools are already built at `tools/image-intelligence/`. Here's what to do:

1. Create a git branch `image-intelligence-run` from current HEAD for safety
2. From `tools/image-intelligence/`, run the analyze-photos CLI in dry-run first:
   ```
   node bin/analyze-photos.js --manifest ../../sites/5-star-roofing/r2-image-manifest.json --output ../../sites/5-star-roofing/image-db.json --industry roofing --concurrency 3 --dry-run
   ```
3. If dry-run looks good (shows ~999 images, ~$3 cost), run it live (same command without --dry-run). It has resume support if it crashes.
4. Run the audit on a few test pages first:
   ```
   node bin/audit-site.js --url https://5starroofingpros.com --pages /commercial-roofing-amarillo/ /tpo-roofing-amarillo/ /hail-damage-repair-amarillo/ --output ../../sites/5-star-roofing/audit-report.json
   ```
5. Run the assignment engine:
   ```
   node bin/assign-images.js --image-db ../../sites/5-star-roofing/image-db.json --app-dir ../../sites/5-star-roofing/app --output ../../sites/5-star-roofing/page-images.json
   ```
6. Dry-run the fix applier from `sites/5-star-roofing/`:
   ```
   node scripts/apply-image-fixes.js --dry-run
   ```
7. If it looks right, apply for real and build:
   ```
   node scripts/apply-image-fixes.js
   npm run build
   ```
8. Commit the results (image-db.json, page-images.json, and any changed pages)

Report status at each step. If anything fails, stop and diagnose before continuing.
